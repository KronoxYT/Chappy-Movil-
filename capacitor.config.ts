import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chappi.app',
  appName: 'Chappi',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;